project: rust-template
version: 0.0.1
stovetop:
  variables: {}
  runtime:
    type: rustc
    version:
  commands:
    build: src/main.rs -o bin/app
    run:
    executable: bin/app
    test:
    clean:
    deploy:
  aliases: {}
  hooks:
  profiles: 
